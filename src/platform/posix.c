/* POSIX glue that does not belong in core. Currently empty: paths.c and
 * relaunch.c cover what the launcher needs, and keeping this file means the
 * seam exists when something genuinely platform-specific turns up.
 */
typedef int dxl_posix_tu_not_empty;
