diff --git a/SurrealEngine/Utils/PerfLog.h b/SurrealEngine/Utils/PerfLog.h
new file mode 100644
--- /dev/null
+++ b/SurrealEngine/Utils/PerfLog.h
@@ -0,0 +1,189 @@
+// TEMPORARY DEBUG TOOL -- revert before commit.
+//
+// Frame-time breakdown for profiling on the handheld. With SURREAL_PERF_LOG=1
+// every 60 frames prints, to stderr, where the average frame went: input,
+// game tick, camera/audio, and rendering -- with rendering split into the
+// time the CPU spends waiting on the GPU (fence), on the swapchain (acquire)
+// and on texture uploads. CPU-bound shows as a big tick/render with a small
+// gpu-wait; GPU-bound as a big gpu-wait.
+#pragma once
+
+#include <chrono>
+#include <cstdint>
+#include <cstdio>
+#include <cstdlib>
+#include <algorithm>
+#include <string>
+#include <unordered_map>
+#include <vector>
+
+namespace PerfLog
+{
+	enum Slot { Input, Tick, View, Render, GpuWait, Acquire, UploadWait, Lightmap, TexUpload, Count };
+
+	inline bool Enabled()
+	{
+		static int enabled = -1;
+		if (enabled < 0)
+		{
+			const char* v = std::getenv("SURREAL_PERF_LOG");
+			enabled = (v && v[0] == '1') ? 1 : 0;
+		}
+		return enabled == 1;
+	}
+
+	inline uint64_t NowNs()
+	{
+		return (uint64_t)std::chrono::duration_cast<std::chrono::nanoseconds>(
+			std::chrono::steady_clock::now().time_since_epoch()).count();
+	}
+
+	struct State
+	{
+		double sum[Count] = {};
+		double worst[Count] = {};
+		double frameSum = 0.0, frameWorst = 0.0;
+		uint64_t frameStart = 0;
+		int frames = 0;
+	};
+
+	inline State& Get() { static State s; return s; }
+
+	// Actor tick time by class, for finding what the game tick spends its time
+	// on. Keyed by class name; count is ticks, not actors.
+	struct ClassCost { double ms = 0.0; int ticks = 0; };
+	inline std::unordered_map<std::string, ClassCost>& Classes()
+	{
+		static std::unordered_map<std::string, ClassCost> c;
+		return c;
+	}
+	inline void AddClass(const std::string& name, double ms)
+	{
+		ClassCost& c = Classes()[name];
+		c.ms += ms;
+		c.ticks++;
+	}
+
+	// Script and native function cost: self time (callees excluded) and call
+	// count, keyed by the UFunction. A stack of child time gives self time.
+	struct FuncCost { double selfMs = 0.0; int calls = 0; bool native = false; std::string name; };
+	inline std::unordered_map<const void*, FuncCost>& Funcs()
+	{
+		static std::unordered_map<const void*, FuncCost> f;
+		return f;
+	}
+	inline std::vector<double>& ChildStack() { static std::vector<double> s; return s; }
+	inline int& TotalCalls() { static int n; return n; }
+
+	struct FuncScope
+	{
+		const void* key;
+		uint64_t start;
+		FuncScope(const void* k) : key(k), start(Enabled() ? NowNs() : 0)
+		{
+			if (start) ChildStack().push_back(0.0);
+		}
+		// Returns true once, the first time a function is seen, so the caller
+		// can supply its name without building a string on every call.
+		template<typename NameFn> void End(bool native, NameFn nameFn)
+		{
+			if (!start) return;
+			double ms = (NowNs() - start) / 1e6;
+			double children = ChildStack().back();
+			ChildStack().pop_back();
+			if (!ChildStack().empty()) ChildStack().back() += ms;
+			FuncCost& c = Funcs()[key];
+			if (c.name.empty()) { c.name = nameFn(); c.native = native; }
+			c.selfMs += ms - children;
+			c.calls++;
+			TotalCalls()++;
+		}
+	};
+
+	// Events per interval: lightmaps rebuilt, textures uploaded.
+	inline int& Counter(int slot) { static int c[Count]; return c[slot]; }
+
+	// SURREAL_PERF_TURN=<aBaseX> makes the player turn on the spot, so a run
+	// with nobody at the pad still brings new surfaces into view.
+	inline float TurnSpeed()
+	{
+		static float speed = -1.0f;
+		if (speed < 0.0f)
+		{
+			const char* v = std::getenv("SURREAL_PERF_TURN");
+			speed = v ? (float)std::atof(v) : 0.0f;
+		}
+		return speed;
+	}
+
+	inline void Add(int slot, double ms)
+	{
+		State& s = Get();
+		s.sum[slot] += ms;
+		if (ms > s.worst[slot]) s.worst[slot] = ms;
+	}
+
+	struct Scope
+	{
+		int slot;
+		uint64_t start;
+		explicit Scope(int s) : slot(s), start(Enabled() ? NowNs() : 0) {}
+		~Scope() { if (start) Add(slot, (NowNs() - start) / 1e6); }
+	};
+
+	// Called once per main-loop iteration.
+	inline void EndFrame()
+	{
+		if (!Enabled())
+			return;
+		State& s = Get();
+		uint64_t now = NowNs();
+		if (s.frameStart)
+		{
+			double ms = (now - s.frameStart) / 1e6;
+			s.frameSum += ms;
+			if (ms > s.frameWorst) s.frameWorst = ms;
+			s.frames++;
+		}
+		s.frameStart = now;
+
+		if (s.frames < 60)
+			return;
+		double n = s.frames;
+		double avg = s.frameSum / n;
+		std::fprintf(stderr,
+			"perf: %.1f fps | frame %.1f (worst %.1f) | input %.1f | tick %.1f (worst %.1f) | view+audio %.1f | "
+			"render %.1f (worst %.1f): gpu-wait %.1f, acquire %.1f, upload-wait %.1f, "
+			"lightmaps %.1f (%d built), tex-upload %.1f (%d), other-cpu %.1f\n",
+			1000.0 / avg, avg, s.frameWorst, s.sum[Input] / n, s.sum[Tick] / n, s.worst[Tick],
+			s.sum[View] / n, s.sum[Render] / n, s.worst[Render], s.sum[GpuWait] / n,
+			s.sum[Acquire] / n, s.sum[UploadWait] / n,
+			s.sum[Lightmap] / n, Counter(Lightmap), s.sum[TexUpload] / n, Counter(TexUpload),
+			(s.sum[Render] - s.sum[GpuWait] - s.sum[Acquire] - s.sum[UploadWait] -
+			 s.sum[Lightmap] - s.sum[TexUpload]) / n);
+		Counter(Lightmap) = Counter(TexUpload) = 0;
+
+		std::vector<std::pair<std::string, ClassCost>> top(Classes().begin(), Classes().end());
+		std::sort(top.begin(), top.end(), [](auto& a, auto& b) { return a.second.ms > b.second.ms; });
+		std::fprintf(stderr, "perf: tick by class (ms/frame, actors):");
+		for (size_t i = 0; i < top.size() && i < 12; i++)
+			std::fprintf(stderr, " %s %.1f (%.0f)", top[i].first.c_str(), top[i].second.ms / n,
+				top[i].second.ticks / n);
+		std::fprintf(stderr, "\n");
+		Classes().clear();
+
+		std::vector<FuncCost> fs;
+		for (auto& it : Funcs()) fs.push_back(it.second);
+		std::sort(fs.begin(), fs.end(), [](auto& a, auto& b) { return a.selfMs > b.selfMs; });
+		std::fprintf(stderr, "perf: %.0f VM calls/frame; top self time (ms/frame, calls/frame):", TotalCalls() / n);
+		for (size_t i = 0; i < fs.size() && i < 20; i++)
+			std::fprintf(stderr, " %s%s %.2f (%.0f)", fs[i].native ? "*" : "", fs[i].name.c_str(),
+				fs[i].selfMs / n, fs[i].calls / n);
+		std::fprintf(stderr, "\n");
+		Funcs().clear();
+		TotalCalls() = 0;
+		std::fflush(stderr);
+		s = State();
+		s.frameStart = now;
+	}
+}
diff --git a/SurrealEngine/Engine.cpp b/SurrealEngine/Engine.cpp
--- a/SurrealEngine/Engine.cpp
+++ b/SurrealEngine/Engine.cpp
@@ -1,6 +1,7 @@
 
 #include "Precomp.h"
 #include "Engine.h"
+#include "Utils/PerfLog.h" // TEMPORARY DEBUG TOOL
 #include "Utils/File.h"
 #include "Utils/StrTools.h"
 #include "Utils/SHA1Sum.h"
@@ -193,9 +194,13 @@
 		LevelInfo->Second() = timedesc->tm_sec;
 		LevelInfo->Millisecond() = 0; // No timedesc equivalent for LevelInfo->Millisecond()
 
+		{ PerfLog::Scope perf(PerfLog::Input); // TEMPORARY DEBUG TOOL
 		UpdateInput(realTimeElapsed);
+		}
 
 		SetPause(!LevelInfo->Pauser().empty());
+
+		{ PerfLog::Scope perfTick(PerfLog::Tick); // TEMPORARY DEBUG TOOL
 
 		// Do NOT pause this Tick event otherwise some messages will stay on screen forever.
 		CallEvent(console, EventName::Tick, { ExpressionValue::FloatValue(levelElapsed) });
@@ -213,6 +218,9 @@
 
 		if (dxRootWindow)
 			dxRootWindow->Tick(levelElapsed); // Should this maybe be realTimeElapsed?
+		} // TEMPORARY DEBUG TOOL: end of the tick scope
+
+		{ PerfLog::Scope perfView(PerfLog::View); // TEMPORARY DEBUG TOOL
 
 		// To do: improve CallEvent so parameter passing isn't this painful
 		UFunction* funcPlayerCalcView = viewport->Actor() ? FindEventFunction(viewport->Actor(), "PlayerCalcView") : nullptr;
@@ -232,9 +240,13 @@
 		}
 
 		UpdateAudio();
+		} // TEMPORARY DEBUG TOOL: end of the view scope
 
 		viewport->SetViewportRect(0, 0, engine->window->GetPixelWidth(), engine->window->GetPixelHeight());
+		{ PerfLog::Scope perfRender(PerfLog::Render); // TEMPORARY DEBUG TOOL
 		render->DrawGame(levelElapsed);
+		}
+		PerfLog::EndFrame(); // TEMPORARY DEBUG TOOL
 
 		// Save the game if there is a request for it
 		if (SaveGameInfo.SaveGameSlot != DONT_SAVE_GAME)
@@ -1557,6 +1569,9 @@
 
 	if (!viewport->Actor())
 		return;
+
+	if (PerfLog::TurnSpeed() != 0.0f) // TEMPORARY DEBUG TOOL
+		activeInputAxes["aBaseX"] = { PerfLog::TurnSpeed(), IK_None };
 
 	for (auto& it : activeInputButtons)
 		viewport->Actor()->SetBool(it.first, true);
diff --git a/SurrealEngine/VM/Frame.cpp b/SurrealEngine/VM/Frame.cpp
--- a/SurrealEngine/VM/Frame.cpp
+++ b/SurrealEngine/VM/Frame.cpp
@@ -1,6 +1,7 @@
 
 #include "Precomp.h"
 #include "Frame.h"
+#include "Utils/PerfLog.h" // TEMPORARY DEBUG TOOL
 #include "Bytecode.h"
 #include "ExpressionEvaluator.h"
 #include "NativeFunc.h"
@@ -230,14 +231,16 @@
 		}
 	}
 
-	if (AllFlags(func->FuncFlags, FunctionFlags::Native))
-	{
-		return CallNative(func, instance, std::move(args));
-	}
-	else
-	{
-		return CallScript(func, instance, std::move(args));
-	}
+	// TEMPORARY DEBUG TOOL: per-function profiling
+	PerfLog::FuncScope perfScope(func);
+	bool perfNative = AllFlags(func->FuncFlags, FunctionFlags::Native);
+	ExpressionValue perfResult = perfNative ? CallNative(func, instance, std::move(args))
+	                                        : CallScript(func, instance, std::move(args));
+	perfScope.End(perfNative, [&]() {
+		// First caller's class: approximate for inherited functions, enough to point at hot spots.
+		return func->Name.ToString() + "[" + (instance->Class ? instance->Class->Name.ToString() : std::string("?")) + "]";
+	});
+	return perfResult;
 }
 
 ExpressionValue Frame::CallScript(UFunction* func, UObject* instance, Array<ExpressionValue> args)
diff --git a/SurrealEngine/Packages/Engine/Resources/Level/ULevel.cpp b/SurrealEngine/Packages/Engine/Resources/Level/ULevel.cpp
--- a/SurrealEngine/Packages/Engine/Resources/Level/ULevel.cpp
+++ b/SurrealEngine/Packages/Engine/Resources/Level/ULevel.cpp
@@ -1,5 +1,7 @@
 
 #include "Precomp.h"
+#include "Utils/PerfLog.h" // TEMPORARY DEBUG TOOL
+#include "Packages/Core/UClass.h" // TEMPORARY DEBUG TOOL
 #include "ULevel.h"
 #include "Packages/Engine/Actors/Info/ULevelInfo.h"
 #include "Packages/Engine/Actors/Pawn/UPlayerPawn.h"
@@ -74,7 +76,10 @@
 	actor->bTicked() = ticked;
 
 	// Tick the actor for this turn
+	uint64_t perfStart = PerfLog::Enabled() ? PerfLog::NowNs() : 0; // TEMPORARY DEBUG TOOL
 	actor->Tick(elapsed);
+	if (perfStart) // TEMPORARY DEBUG TOOL
+		PerfLog::AddClass(actor->Class->Name.ToString(), (PerfLog::NowNs() - perfStart) / 1e6);
 
 	// Destroy the actor if its time
 	if (actor->Role() >= ROLE_SimulatedProxy && actor->LifeSpan() != 0.0f)
diff --git a/SurrealEngine/Light/LightSystem_Light.cpp b/SurrealEngine/Light/LightSystem_Light.cpp
--- a/SurrealEngine/Light/LightSystem_Light.cpp
+++ b/SurrealEngine/Light/LightSystem_Light.cpp
@@ -1,6 +1,7 @@
 
 #include "Precomp.h"
 #include "LightSystem.h"
+#include "Utils/PerfLog.h" // TEMPORARY DEBUG TOOL
 #include "RenderDevice/RenderDevice.h"
 #include "Engine.h"
 #include "Render/RenderSubsystem.h"
@@ -159,6 +160,7 @@
 	auto& lmtexture = lmtextures[cacheID];
 	if (!lmtexture || lmtexture->LastStaticUpdate != lastStaticUpdate || lmtexture->LastDynamicUpdate != lastDynamicUpdate)
 	{
+		PerfLog::Scope perfLm(PerfLog::Lightmap); PerfLog::Counter(PerfLog::Lightmap)++; // TEMPORARY DEBUG TOOL
 		engine->render->Stats.LightmapsUpdated++;
 
 		Builder.Setup(model, coords, lightmapIndex);
diff --git a/SurrealEngine/RenderDevice/Vulkan/CommandBufferManager.cpp b/SurrealEngine/RenderDevice/Vulkan/CommandBufferManager.cpp
--- a/SurrealEngine/RenderDevice/Vulkan/CommandBufferManager.cpp
+++ b/SurrealEngine/RenderDevice/Vulkan/CommandBufferManager.cpp
@@ -1,6 +1,7 @@
 
 #include "Precomp.h"
 #include "CommandBufferManager.h"
+#include "Utils/PerfLog.h" // TEMPORARY DEBUG TOOL
 #include "VulkanRenderDevice.h"
 #include <surrealgpu/vulkanswapchain.h>
 
@@ -44,6 +45,7 @@
 			.AddCommandBuffer(TransferCommands.get())
 			.Execute(renderer->Device.get(), renderer->Device.get()->GraphicsQueue, RenderFinishedFence.get());
 
+		PerfLog::Scope perf(PerfLog::UploadWait); // TEMPORARY DEBUG TOOL
 		VkResult result = vkWaitForFences(renderer->Device.get()->device, 1, &RenderFinishedFence->fence, VK_TRUE, std::numeric_limits<uint64_t>::max());
 		if (result != VK_SUCCESS)
 			throw std::runtime_error("vkWaitForFences failed");
@@ -78,7 +80,9 @@
 			}
 		}
 
+		{ PerfLog::Scope perf(PerfLog::Acquire); // TEMPORARY DEBUG TOOL
 		PresentImageIndex = SwapChain->AcquireImage(ImageAvailableSemaphore.get());
+		}
 		if (PresentImageIndex != -1)
 		{
 			renderer->DrawPresentTexture(presentWidth, presentHeight);
@@ -119,6 +123,7 @@
 		SwapChain->QueuePresent(PresentImageIndex, RenderFinishedSemaphores[PresentImageIndex].get());
 	}
 
+	PerfLog::Scope perfWait(PerfLog::GpuWait); // TEMPORARY DEBUG TOOL
 	VkResult result = vkWaitForFences(renderer->Device.get()->device, 1, &RenderFinishedFence->fence, VK_TRUE, std::numeric_limits<uint64_t>::max());
 	if (result != VK_SUCCESS)
 		throw std::runtime_error("vkWaitForFences failed");
diff --git a/SurrealEngine/RenderDevice/Vulkan/TextureManager.cpp b/SurrealEngine/RenderDevice/Vulkan/TextureManager.cpp
--- a/SurrealEngine/RenderDevice/Vulkan/TextureManager.cpp
+++ b/SurrealEngine/RenderDevice/Vulkan/TextureManager.cpp
@@ -1,6 +1,7 @@
 
 #include "Precomp.h"
 #include "TextureManager.h"
+#include "Utils/PerfLog.h" // TEMPORARY DEBUG TOOL
 #include "VulkanRenderDevice.h"
 #include "CachedTexture.h"
 #include "Packages/Engine/Resources/Level/UModel.h"
@@ -40,11 +41,13 @@
 	std::unique_ptr<CachedTexture>& tex = TextureCache[(int)masked][info->CacheID];
 	if (!tex)
 	{
+		PerfLog::Scope perf(PerfLog::TexUpload); PerfLog::Counter(PerfLog::TexUpload)++; // TEMPORARY DEBUG TOOL
 		tex.reset(new CachedTexture());
 		renderer->Uploads->UploadTexture(tex.get(), *info, masked);
 	}
 	else if (info->bRealtimeChanged)
 	{
+		PerfLog::Scope perf(PerfLog::TexUpload); PerfLog::Counter(PerfLog::TexUpload)++; // TEMPORARY DEBUG TOOL
 		info->bRealtimeChanged = 0;
 		renderer->Uploads->UploadTexture(tex.get(), *info, masked);
 	}
